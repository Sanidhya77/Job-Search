"""LLM-based reasoning modules.

These modules are the agent's reasoning core. They call the OpenAI
chat completions endpoint through the LLMClient wrapper and return
typed dataclasses to the agent.
"""
